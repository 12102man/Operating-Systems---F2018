# Kill all docker containers
docker kill $(docker ps -a -q)
docker rm $(docker ps -a -q)

# Create container
docker build -t operating_systems .

# Run container
docker run -d -t -i --name operating_systems -v /Users/mikhailtkachenko/Desktop/Docker/OS/common:/common operating_systems

# Get inside container
docker exec -it operatig_systems /bin/bash
